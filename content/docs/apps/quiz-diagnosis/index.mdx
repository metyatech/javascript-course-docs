---
title: 診断アプリを作ろう
authoringMode: 'tutorial'
---

3つの質問に答えると、結果が変わる診断アプリを作ります。

このページでは、ただコードを写すのではなく、次の流れで「診断の仕組み」を作ります。

1. 質問と選択肢を設計する
2. `select.value` で選ばれた答えを読む
3. 答えに応じて点数を増やす
4. 点数によって結果を分ける
5. テーマを変えて自分の診断にする

---

<Section title="今日のミッション" goal="選択肢の値を使って、結果が変わる診断アプリを作ります。">

完成すると、3つの質問に答えてボタンを押したとき、結果タイトル・コメント・背景色が変わります。

<CodePreview
  sourceId="quiz-diagnosis-final-preview"
  htmlVisible={false}
  cssVisible={false}
  jsVisible={false}
  previewVisible={true}
  share={false}
>

```html
<main class="app">
    <h1>休日タイプ診断</h1>
    <p>質問に答えて、あなたの休日タイプを診断します。</p>

    <section class="question">
        <label for="q1">Q1. 休日の朝は？</label>
        <select id="q1">
            <option value="relax">ゆっくり寝たい</option>
            <option value="active">早起きして出かけたい</option>
        </select>
    </section>

    <section class="question">
        <label for="q2">Q2. 好きな過ごし方は？</label>
        <select id="q2">
            <option value="relax">家でのんびり</option>
            <option value="active">外で活動する</option>
        </select>
    </section>

    <section class="question">
        <label for="q3">Q3. 友達に誘われたら？</label>
        <select id="q3">
            <option value="relax">予定が合えば行く</option>
            <option value="active">すぐ行きたい</option>
        </select>
    </section>

    <button id="diagnoseButton">診断する</button>

    <section id="resultBox" class="result-box">
        <h2 id="resultTitle">診断結果</h2>
        <p id="resultComment">ボタンを押すと結果が表示されます。</p>
    </section>
</main>
```

```css
* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: sans-serif;
    background: #eef2f7;
    color: #222;
}

.app {
    width: min(720px, 92%);
    margin: 32px auto;
    padding: 24px;
    background: white;
    border-radius: 20px;
}

.question {
    margin: 20px 0;
}

label {
    display: block;
    margin-bottom: 8px;
    font-weight: bold;
}

select {
    width: 100%;
    padding: 12px;
    font-size: 16px;
}

button {
    width: 100%;
    padding: 16px;
    margin-top: 16px;
    border: none;
    border-radius: 999px;
    font-size: 18px;
    cursor: pointer;
}

.result-box {
    margin-top: 24px;
    padding: 20px;
    border-radius: 16px;
    background: #eeeeee;
}

.relax {
    background: #d8f3dc;
}

.balance {
    background: #fff3cd;
}

.active {
    background: #d7e3fc;
}
```

```js
const q1 = document.querySelector('#q1');
const q2 = document.querySelector('#q2');
const q3 = document.querySelector('#q3');

const button = document.querySelector('#diagnoseButton');
const resultBox = document.querySelector('#resultBox');
const resultTitle = document.querySelector('#resultTitle');
const resultComment = document.querySelector('#resultComment');

button.addEventListener('click', () => {
    let score = 0;

    if (q1.value === 'active') {
        score++;
    }

    if (q2.value === 'active') {
        score++;
    }

    if (q3.value === 'active') {
        score++;
    }

    resultBox.className = 'result-box';

    if (score <= 1) {
        resultTitle.textContent = 'のんびりタイプ';
        resultComment.textContent = '家でゆっくり過ごすと元気になれそうです。';
        resultBox.classList.add('relax');
    } else if (score === 2) {
        resultTitle.textContent = 'バランスタイプ';
        resultComment.textContent = '休む時間と遊ぶ時間のバランスを取るのが得意です。';
        resultBox.classList.add('balance');
    } else {
        resultTitle.textContent = 'アクティブタイプ';
        resultComment.textContent = '外に出て動くことでリフレッシュできそうです。';
        resultBox.classList.add('active');
    }
});
```

</CodePreview>

<Exercise>

完成形を動かして、次のことを確認してください。

1. 質問の答えを変えると、結果も変わるか
2. 同じ答えの組み合わせなら、同じ結果になるか
3. 結果の文章だけでなく、背景色も変わるか

<Solution>

この診断はランダムではありません。  
選んだ答えから `score` を計算しているので、同じ答えの組み合わせなら同じ結果になります。

</Solution>

</Exercise>

</Section>

---

<Section title="診断のルールを先に設計する" goal="HTMLを書く前に、どの答えをどう判定するか決めます。">

診断アプリでは、見た目より先に「判定ルール」を決めます。

今回のルールは、`active` を選んだ数で結果を分ける形にします。

| active の数 | 結果 |
| --- | --- |
| 0〜1個 | のんびりタイプ |
| 2個 | バランスタイプ |
| 3個 | アクティブタイプ |

<Concept title="value は診断用の目印">

画面に表示する文字は日本語で構いません。  
ただし、JavaScriptで判定するときは、短くて分かりやすい値を使うと扱いやすくなります。

```html
<option value="active">早起きして出かけたい</option>
```

この場合、画面には「早起きして出かけたい」と表示されます。  
JavaScriptで取り出す値は `"active"` です。

</Concept>

<Exercise>

次の選択肢を選んだとき、`q1.value` は何になるでしょうか。

```html
<select id="q1">
    <option value="relax">ゆっくり寝たい</option>
    <option value="active">早起きして出かけたい</option>
</select>
```

「早起きして出かけたい」を選んだ場合を考えてください。

<Solution>

`q1.value` は `"active"` になります。  
表示されている文字ではなく、選ばれた `<option>` の `value` 属性の値が取り出されます。

</Solution>

</Exercise>

</Section>

---

<Section title="HTMLで質問フォームを作る" goal="質問、選択肢、結果表示エリアを用意します。">

3つの質問は、同じ形で作ります。  
それぞれの `<select>` に `id` を付けて、JavaScriptから取得できるようにします。

<Action title="HTMLを書く">

`<body>` の中に次のHTMLを書きます。

```html
<main class="app">
    <h1>休日タイプ診断</h1>
    <p>質問に答えて、あなたの休日タイプを診断します。</p>

    <section class="question">
        <label for="q1">Q1. 休日の朝は？</label>
        <select id="q1">
            <option value="relax">ゆっくり寝たい</option>
            <option value="active">早起きして出かけたい</option>
        </select>
    </section>

    <section class="question">
        <label for="q2">Q2. 好きな過ごし方は？</label>
        <select id="q2">
            <option value="relax">家でのんびり</option>
            <option value="active">外で活動する</option>
        </select>
    </section>

    <section class="question">
        <label for="q3">Q3. 友達に誘われたら？</label>
        <select id="q3">
            <option value="relax">予定が合えば行く</option>
            <option value="active">すぐ行きたい</option>
        </select>
    </section>

    <button id="diagnoseButton">診断する</button>

    <section id="resultBox" class="result-box">
        <h2 id="resultTitle">診断結果</h2>
        <p id="resultComment">ボタンを押すと結果が表示されます。</p>
    </section>
</main>
```

</Action>

<Action title="CSSとJavaScriptを読み込む">

`index.html` の `<head>` の中に、CSSとJavaScriptの読み込みを書きます。`script` に `defer` を付けると、HTMLを読み込み終えてからJavaScriptが実行されます。

```html
<head>
    <link rel="stylesheet" href="style.css">
    <script src="script.js" defer></script>
</head>
```

`style.css` と `script.js` は、このあとのステップで作ります。

</Action>

<Exercise>

JavaScriptから取得する必要がある `id` をすべて書き出してください。

<Solution>

取得する必要がある `id` は次の7つです。

- `q1`
- `q2`
- `q3`
- `diagnoseButton`
- `resultBox`
- `resultTitle`
- `resultComment`

</Solution>

</Exercise>

<Verify title="部品を確認する">

3つの質問、診断ボタン、結果表示エリアが表示されていれば成功です。  
この時点では、診断ボタンを押してもまだ結果は変わりません。

</Verify>

</Section>

---

<Section title="CSSで結果の色を準備する" goal="JavaScriptからクラスを追加するだけで色を変えられるようにします。">

結果の色は、JavaScriptで直接色を書くのではなく、CSSのクラスとして用意しておきます。

<Action title="CSSを書く">

`style.css` に次のCSSを書きます。

```css
* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: sans-serif;
    background: #eef2f7;
    color: #222;
}

.app {
    width: min(720px, 92%);
    margin: 32px auto;
    padding: 24px;
    background: white;
    border-radius: 20px;
}

.question {
    margin: 20px 0;
}

label {
    display: block;
    margin-bottom: 8px;
    font-weight: bold;
}

select {
    width: 100%;
    padding: 12px;
    font-size: 16px;
}

button {
    width: 100%;
    padding: 16px;
    margin-top: 16px;
    border: none;
    border-radius: 999px;
    font-size: 18px;
    cursor: pointer;
}

.result-box {
    margin-top: 24px;
    padding: 20px;
    border-radius: 16px;
    background: #eeeeee;
}

.relax {
    background: #d8f3dc;
}

.balance {
    background: #fff3cd;
}

.active {
    background: #d7e3fc;
}
```

</Action>

<Concept title="class を切り替える理由">

診断結果によって色を変えたい場合、JavaScriptでCSSのクラスを付け替えると分かりやすくなります。

- のんびりタイプなら `.relax`
- バランスタイプなら `.balance`
- アクティブタイプなら `.active`

このように、結果と見た目を対応させます。

</Concept>

<Verify title="見た目を確認する">

白いカードの中に質問とボタン、グレーの結果ボックスが表示されていれば成功です。

</Verify>

</Section>

---

<Section title="選ばれた答えを読み取る" goal="select.value で、今選ばれている値を取得します。">

まずは、結果を変える前に、選ばれた値をコンソールで確認します。

<Action title="要素を取得する">

`script.js` に次のコードを書きます。

```js
const q1 = document.querySelector('#q1');
const q2 = document.querySelector('#q2');
const q3 = document.querySelector('#q3');

const button = document.querySelector('#diagnoseButton');
const resultBox = document.querySelector('#resultBox');
const resultTitle = document.querySelector('#resultTitle');
const resultComment = document.querySelector('#resultComment');
```

</Action>

<Action title="ボタンを押したときに値を確認する">

続けて、次のコードを書きます。

```js
button.addEventListener('click', () => {
    console.log(q1.value);
    console.log(q2.value);
    console.log(q3.value);
});
```

</Action>

<Verify title="値を確認する">

質問の答えを変えてから「診断する」ボタンを押し、コンソールに `relax` または `active` が表示されれば成功です。

</Verify>

<Recovery title="undefined やエラーが出るとき">

HTML側の `id` と、JavaScriptの `document.querySelector(...)` の名前が一致しているか確認します。  
特に `#diagnoseButton` の大文字小文字に注意してください。

</Recovery>

</Section>

---

<Section title="score を計算する" goal="active を選んだ数だけ点数を増やします。">

診断結果を決めるために、`score` という変数を使います。  
最初は0にして、`active` が選ばれていたら1ずつ増やします。

<Action title="score を計算する処理を書く">

クリックイベントの中を次のように書き換えます。

```js
button.addEventListener('click', () => {
    let score = 0;

    if (q1.value === 'active') {
        score++;
    }

    if (q2.value === 'active') {
        score++;
    }

    if (q3.value === 'active') {
        score++;
    }

    console.log(score);
});
```

</Action>

<Exercise>

3つの質問で、次のように選んだ場合、`score` はいくつになりますか。

| q1 | q2 | q3 |
| --- | --- | --- |
| active | relax | active |

<Solution>

`score` は `2` になります。  
`active` が選ばれているのは q1 と q3 の2つだからです。

</Solution>

</Exercise>

<Verify title="点数を確認する">

3つすべてを active 側にすると `3`、すべて relax 側にすると `0` がコンソールに表示されれば成功です。

</Verify>

</Section>

---

<Section title="点数で結果を切り替える" goal="score の値に応じて、表示する結果を変えます。">

`score` が計算できたら、次は結果表示です。  
文字だけでなく、結果ボックスの色も一緒に変えます。

<Concept title="className と classList.add">

結果を切り替えるたびに、前回の色クラスを消す必要があります。  
そのため、まず `className` で基本クラスだけに戻します。

```js
resultBox.className = 'result-box';
```

その後、結果に合うクラスを追加します。

```js
resultBox.classList.add('active');
```

</Concept>

<Action title="判定処理を追加する">

クリックイベントの中の `console.log(score);` を削除し、次の判定処理を追加します。

```js
    resultBox.className = 'result-box';

    if (score <= 1) {
        resultTitle.textContent = 'のんびりタイプ';
        resultComment.textContent = '家でゆっくり過ごすと元気になれそうです。';
        resultBox.classList.add('relax');
    } else if (score === 2) {
        resultTitle.textContent = 'バランスタイプ';
        resultComment.textContent = '休む時間と遊ぶ時間のバランスを取るのが得意です。';
        resultBox.classList.add('balance');
    } else {
        resultTitle.textContent = 'アクティブタイプ';
        resultComment.textContent = '外に出て動くことでリフレッシュできそうです。';
        resultBox.classList.add('active');
    }
```

</Action>

<Verify title="結果を確認する">

次の3パターンを確認してください。

| 選び方 | 期待される結果 |
| --- | --- |
| active が0〜1個 | のんびりタイプ |
| active が2個 | バランスタイプ |
| active が3個 | アクティブタイプ |

同じ組み合わせで2回押しても色がおかしくならなければ成功です。

</Verify>

</Section>

---

<Section title="バグ修正チャレンジ" goal="診断アプリで起きやすいミスを直します。">

次のコードには、よくあるミスが4つ入っています。

<Exercise>

```js
button.addEventListener('click', () => {
    let score = 0;

    if (q1.value = 'active') {
        score++;
    }

    if (q2.value === active) {
        score++;
    }

    if (q3.value === 'active') {
        score++;
    }

    resultBox.classList.add('result-box');

    if (score = 2) {
        resultTitle.textContent = 'バランスタイプ';
    }
});
```

<Solution>

1. `q1.value = 'active'` は代入なので、`q1.value === 'active'` に直す。
2. `active` がクォートで囲まれていないので、`'active'` に直す。
3. `resultBox.classList.add('result-box')` では前回の色クラスが消えないので、`resultBox.className = 'result-box';` にする。
4. `if (score = 2)` も代入なので、`if (score === 2)` に直す。

</Solution>

</Exercise>

</Section>

---

<Section title="改造ミッション" goal="テーマ・質問・結果を変えて、自分だけの診断アプリにします。">

最後は、休日タイプ診断を別のテーマに作り変えます。  
次の中から1つ選んでください。

| ミッション | 結果例 |
| --- | --- |
| 今日のテンション診断 | 超高テンション / いつも通り / おだやか |
| 勉強スタイル診断 | 集中型 / バランス型 / コツコツ型 |
| ゲームプレイ診断 | 攻撃型 / サポート型 / 探索型 |
| おすすめ昼ごはん診断 | がっつり / ふつう / 軽め |

### 演習1

<Exercise>

選んだテーマに合わせて、次の表を先に作ってください。

| 結果 | score の条件 | 表示するコメント | CSSクラス |
| --- | --- | --- | --- |
| 例：超高テンション | 5以上 | 今日はアクティブに動けそうです。 | `high-result` |

<Solution>

「今日のテンション診断」にする場合は、次のように設計できます。

| 結果 | score の条件 | 表示するコメント | CSSクラス |
| --- | --- | --- | --- |
| 超高テンション | 3（全問 active） | 今日はアクティブに動けそうです。 | `high-result` |
| いつも通り | 1〜2 | マイペースに過ごせそうです。 | `mid-result` |
| おだやか | 0 | ゆっくり休むのが合いそうです。 | `low-result` |

表を先に完成させてからJavaScriptを書くと、`if` の条件をそのまま使えます。

</Solution>

</Exercise>

### 演習2

<Exercise>

質問を3つ作り、各選択肢の `value` を決めてください。  
そのあと、HTML、CSS、JavaScriptを書き換えます。

<Solution>

「今日のテンション診断」にする場合は、次のような作り方ができます。

```html
<h1>今日のテンション診断</h1>
<p>3つの質問に答えて、今の気分をチェックしましょう。</p>
```

```html
<section class="question">
    <label for="q1">Q1. 今の気分は？</label>
    <select id="q1">
        <option value="high">ノリノリ</option>
        <option value="mid">ふつう</option>
        <option value="low">ちょっとおだやか</option>
    </select>
</section>
```

```css
.high-result {
    background: #ffd6a5;
}

.mid-result {
    background: #caffbf;
}

.low-result {
    background: #bdb2ff;
}
```

```js
button.addEventListener('click', () => {
    let score = 0;

    if (q1.value === 'high') {
        score += 2;
    } else if (q1.value === 'mid') {
        score += 1;
    }

    if (q2.value === 'high') {
        score += 2;
    } else if (q2.value === 'mid') {
        score += 1;
    }

    if (q3.value === 'high') {
        score += 2;
    } else if (q3.value === 'mid') {
        score += 1;
    }

    resultBox.className = 'result-box';

    if (score >= 5) {
        resultTitle.textContent = '超高テンション';
        resultComment.textContent = '今日はアクティブに動けそうです。';
        resultBox.classList.add('high-result');
    } else if (score >= 3) {
        resultTitle.textContent = 'いつも通り';
        resultComment.textContent = 'マイペースに過ごせそうです。';
        resultBox.classList.add('mid-result');
    } else {
        resultTitle.textContent = 'おだやか';
        resultComment.textContent = 'ゆっくり休むのが合いそうです。';
        resultBox.classList.add('low-result');
    }
});
```

</Solution>

</Exercise>

### 演習-発展1

<Exercise>

余裕がある人は、質問を4問以上に増やしてみましょう。`score` の条件（結果の境目）も合わせて見直します。

<Solution>

4問目のHTMLを追加し、JavaScriptで取得・判定します。

```html
<section class="question">
    <label for="q4">Q4. 休日の過ごし方で外出はどのくらい？</label>
    <select id="q4">
        <option value="relax">ほとんど家にいる</option>
        <option value="active">外に出ることが多い</option>
    </select>
</section>
```

```js
const q4 = document.querySelector('#q4');
```

クリックイベントの中に判定を追加します。

```js
if (q4.value === 'active') {
    score++;
}
```

`score` の最大が 4 になるため、結果の境界を見直します。

| active の数 | 結果 |
| --- | --- |
| 0〜1個 | のんびりタイプ |
| 2〜3個 | バランスタイプ |
| 4個 | アクティブタイプ |

```js
if (score <= 1) {
    // のんびりタイプ
} else if (score <= 3) {
    // バランスタイプ
} else {
    // アクティブタイプ
}
```

</Solution>

</Exercise>

<Verify title="完成確認">

次の条件をすべて満たしていれば完成です。

- 質問が3つある
- 選択肢を変えると結果が変わる
- 結果が3種類ある
- 結果ごとに背景色が変わる
- テーマが最初から最後まで統一されている

</Verify>

</Section>
