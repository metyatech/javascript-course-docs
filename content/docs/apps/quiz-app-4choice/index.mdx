---
title: 4択クイズアプリを作ろう
---

import Exercise, { QuickCheck, Hint, Answer } from '@metyatech/exercise/client';

4つの選択肢から答えを選び、最後に正解数を表示するクイズアプリを作ります。

完成形を確認したあと、JavaScriptを少しずつ追加して仕組みを作ります。  
演習1〜8が基本課題です。発展演習は、時間に余裕があれば取り組みます。

---

<Section title="完成形を確認する" goal="完成形を操作し、回答前・回答後・全問終了後の動きを確認します。">

まずは完成形を動かし、これから作る機能を確認します。

<CodePreview
  sourceId="quiz-app-4choice-final-preview"
  htmlVisible={false}
  cssVisible={false}
  jsVisible={false}
  previewVisible={true}
  share={false}
>

```html
<main class="app">
    <h1>4択クイズアプリ</h1>

    <section class="quiz-box">
        <p id="progressText">1問目</p>
        <h2 id="questionText">問題文が入ります</h2>

        <div id="choices">
            <button id="choice1">選択肢1</button>
            <button id="choice2">選択肢2</button>
            <button id="choice3">選択肢3</button>
            <button id="choice4">選択肢4</button>
        </div>

        <p id="messageText">選択肢を選んでください。</p>
    </section>

    <button id="actionButton">次の問題へ</button>
</main>
```

```css
* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: sans-serif;
    background: #f6f7fb;
    color: #222222;
}

.app {
    width: 92%;
    max-width: 760px;
    margin: 32px auto;
    padding: 24px;
    background: white;
    border-radius: 20px;
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.08);
}

.quiz-box {
    padding: 20px;
    border-radius: 16px;
    background: #f0f0f0;
}

#progressText {
    font-weight: bold;
}

#questionText {
    min-height: 64px;
}

#choices button {
    display: block;
    width: 100%;
    margin-top: 12px;
}

button {
    padding: 14px;
    border: 2px solid transparent;
    border-radius: 12px;
    font-size: 16px;
    cursor: pointer;
}

button:hover {
    border-color: #555555;
}

button:disabled {
    cursor: not-allowed;
    opacity: 0.75;
}

#actionButton {
    width: 100%;
    margin-top: 20px;
    border-radius: 999px;
}

.hidden {
    display: none;
}

.correct {
    background: #ccebd3;
    color: #123b1d;
}

.wrong {
    background: #f3c8cc;
    color: #50151b;
}
```

```js
let questions = [
    {
        text: '次のうち、HTML要素にイベント処理を登録するメソッドは？',
        choices: ['querySelector', 'addEventListener', 'textContent', 'console.log'],
        answerIndex: 1
    },
    {
        text: '配列の最初の要素を取り出すインデックスは？',
        choices: ['0', '1', '2', '3'],
        answerIndex: 0
    },
    {
        text: '次のうち、HTML要素の文字を取得・設定できるプロパティは？',
        choices: ['value', 'length', 'textContent', 'querySelector'],
        answerIndex: 2
    }
];

let currentIndex = 0;
let score = 0;
let answered = false;

let progressText = document.querySelector('#progressText');
let questionText = document.querySelector('#questionText');
let choices = document.querySelector('#choices');
let messageText = document.querySelector('#messageText');
let actionButton = document.querySelector('#actionButton');

let choice1 = document.querySelector('#choice1');
let choice2 = document.querySelector('#choice2');
let choice3 = document.querySelector('#choice3');
let choice4 = document.querySelector('#choice4');

let choiceButtons = [choice1, choice2, choice3, choice4];

function showQuestion() {
    let question = questions[currentIndex];

    progressText.textContent = (currentIndex + 1) + '問目 / ' + questions.length + '問中';
    questionText.textContent = question.text;

    for (let i = 0; i < choiceButtons.length; i++) {
        choiceButtons[i].textContent = question.choices[i];
        choiceButtons[i].disabled = false;
        choiceButtons[i].classList.remove('correct');
        choiceButtons[i].classList.remove('wrong');
    }

    choices.classList.remove('hidden');
    messageText.textContent = '選択肢を選んでください。';
    actionButton.textContent = '次の問題へ';
    answered = false;
}

function checkAnswer(selectedIndex) {
    let question = questions[currentIndex];
    answered = true;

    for (let i = 0; i < choiceButtons.length; i++) {
        choiceButtons[i].disabled = true;
    }

    choiceButtons[question.answerIndex].classList.add('correct');

    if (selectedIndex === question.answerIndex) {
        score++;
        messageText.textContent = '正解！';
    } else {
        choiceButtons[selectedIndex].classList.add('wrong');
        let correctAnswer = question.choices[question.answerIndex];
        messageText.textContent = '不正解。正解は「' + correctAnswer + '」です。';
    }
}

function showFinalResult() {
    progressText.textContent = '終了';
    questionText.textContent = '結果は ' + questions.length + '問中 ' + score + '問正解です。';
    choices.classList.add('hidden');
    messageText.textContent = 'おつかれさまでした。';
    actionButton.textContent = 'もう一度';
}

choice1.addEventListener('click', function () {
    checkAnswer(0);
});

choice2.addEventListener('click', function () {
    checkAnswer(1);
});

choice3.addEventListener('click', function () {
    checkAnswer(2);
});

choice4.addEventListener('click', function () {
    checkAnswer(3);
});

actionButton.addEventListener('click', function () {
    if (currentIndex === questions.length) {
        currentIndex = 0;
        score = 0;
        showQuestion();
        return;
    }

    if (answered === false) {
        messageText.textContent = '先に選択肢を選んでください。';
        return;
    }

    currentIndex++;

    if (currentIndex < questions.length) {
        showQuestion();
    } else {
        showFinalResult();
    }
});

showQuestion();
```

</CodePreview>

<Verify title="完成形の動きを確認する">

完成形を、次の順番で操作してください。

1. 回答前に「次の問題へ」を押し、問題が進まないことを確認する
2. 1回目は全問正解し、正解の選択肢が緑色になることと、回答後に選択肢を押せないことを確認する
3. 結果が「3問中3問正解」になり、同じボタンの文字が「もう一度」に変わることを確認する
4. 「もう一度」を押し、1問目・0点から再開して、ボタンの文字が「次の問題へ」に戻ることを確認する
5. 2回目は全問不正解にし、選んだ選択肢が赤色、正解が緑色になり、結果が「3問中0問正解」になることを確認する

すべて確認できれば成功です。

</Verify>

</Section>

---

<Section title="HTMLとCSSを用意する" goal="クイズの画面と、JavaScriptを読み込む準備をします。">

HTMLとCSSはコピーして使います。JavaScriptは空の状態から作ります。

<Action title="3つのファイルを用意する">

作業フォルダに、次の3つのファイルを用意します。

- `index.html`
- `style.css`
- `script.js`

</Action>

<Action title="CSSとJavaScriptを読み込む">

`index.html` の `<head>` の中に、次の2行を追加します。

```html
<link rel="stylesheet" href="style.css">
<script src="script.js" defer></script>
```

`defer` を付けると、HTMLの読み込みが終わったあとでJavaScriptが実行されます。

</Action>

<Action title="HTMLをコピーする">

`index.html` の `<body>` の中に、次のHTMLをコピーして貼り付けます。  
画面下の操作ボタンは1つだけ用意し、表示する文字をJavaScriptで切り替えます。

```html
<main class="app">
    <h1>4択クイズアプリ</h1>

    <section class="quiz-box">
        <p id="progressText">1問目</p>
        <h2 id="questionText">問題文が入ります</h2>

        <div id="choices">
            <button id="choice1">選択肢1</button>
            <button id="choice2">選択肢2</button>
            <button id="choice3">選択肢3</button>
            <button id="choice4">選択肢4</button>
        </div>

        <p id="messageText">選択肢を選んでください。</p>
    </section>

    <button id="actionButton">次の問題へ</button>
</main>
```

</Action>

<Action title="CSSをコピーする">

`style.css` に次のCSSをコピーして貼り付けます。

```css
* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: sans-serif;
    background: #f6f7fb;
    color: #222222;
}

.app {
    width: 92%;
    max-width: 760px;
    margin: 32px auto;
    padding: 24px;
    background: white;
    border-radius: 20px;
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.08);
}

.quiz-box {
    padding: 20px;
    border-radius: 16px;
    background: #f0f0f0;
}

#progressText {
    font-weight: bold;
}

#questionText {
    min-height: 64px;
}

#choices button {
    display: block;
    width: 100%;
    margin-top: 12px;
}

button {
    padding: 14px;
    border: 2px solid transparent;
    border-radius: 12px;
    font-size: 16px;
    cursor: pointer;
}

button:hover {
    border-color: #555555;
}

button:disabled {
    cursor: not-allowed;
    opacity: 0.75;
}

#actionButton {
    width: 100%;
    margin-top: 20px;
    border-radius: 999px;
}

.hidden {
    display: none;
}

.correct {
    background: #ccebd3;
    color: #123b1d;
}

.wrong {
    background: #f3c8cc;
    color: #50151b;
}
```

</Action>

<Verify title="画面を確認する">

問題文、4つの選択肢、「次の問題へ」ボタンが表示されていれば成功です。  

</Verify>

<Recovery title="見た目が完成形と違う場合">

次の箇所を確認してください。

1. `index.html` の `<head>` で `style.css` を読み込んでいるか
2. HTMLの閉じタグが抜けていないか
3. CSSの波かっこが抜けていないか

</Recovery>

</Section>

---

<Section title="問題データを用意する" goal="問題文、選択肢、正解のインデックスを配列とオブジェクトでまとめます。">

1問分のデータは、次の3つのプロパティを持つオブジェクトにします。

| プロパティ | 入れる値 |
| --- | --- |
| `text` | 問題文 |
| `choices` | 4つの選択肢を入れた配列 |
| `answerIndex` | 正解の選択肢のインデックス |

配列のインデックスは `0` から始まります。正解が2番目なら、`answerIndex` は `1` です。

<Action title="questions配列を書く">

空の `script.js` に、次のコードを書きます。

```js
let questions = [
    {
        text: '次のうち、HTML要素にイベント処理を登録するメソッドは？',
        choices: ['querySelector', 'addEventListener', 'textContent', 'console.log'],
        answerIndex: 1
    },
    {
        text: '配列の最初の要素を取り出すインデックスは？',
        choices: ['0', '1', '2', '3'],
        answerIndex: 0
    },
    {
        text: '次のうち、HTML要素の文字を取得・設定できるプロパティは？',
        choices: ['value', 'length', 'textContent', 'querySelector'],
        answerIndex: 2
    }
];
```

</Action>

### 理解確認1

<QuickCheck>

次の問題では、正解は4番目の `length` です。`answerIndex` に入れる数値を答えてください。

```js
{
    text: '配列の要素数を表すプロパティは？',
    choices: ['value', 'textContent', 'querySelector', 'length'],
    answerIndex: ___
}
```

<Hint>

配列のインデックスは、1番目から順に `0`、`1`、`2`、`3` と数えます。

</Hint>

<Answer>

`3` です。4番目の選択肢のインデックスは、`0` から数えるため `3` になります。

</Answer>

</QuickCheck>

</Section>

---

<Section title="クイズの進み具合を変数で覚える" goal="現在の問題、正解数、回答済みかどうかを変数で管理します。">

クイズの進み具合を表す値を、3つの変数で覚えます。

| 変数 | 最初の値 | 役割 |
| --- | ---: | --- |
| `currentIndex` | `0` | 表示中の問題のインデックス |
| `score` | `0` | 正解数 |
| `answered` | `false` | 今の問題に回答したか |

<Action title="3つの変数を書く">

`questions` 配列の下に、次のコードを追加します。

```js
let currentIndex = 0;
let score = 0;
let answered = false;
```

</Action>

<Action title="HTML要素を取得する">

3つの変数の下に、次のコードを追加します。

```js
let progressText = document.querySelector('#progressText');
let questionText = document.querySelector('#questionText');
let choices = document.querySelector('#choices');
let messageText = document.querySelector('#messageText');
let actionButton = document.querySelector('#actionButton');

let choice1 = document.querySelector('#choice1');
let choice2 = document.querySelector('#choice2');
let choice3 = document.querySelector('#choice3');
let choice4 = document.querySelector('#choice4');

let choiceButtons = [choice1, choice2, choice3, choice4];
```

`actionButton` は、表示する文字を変えて「次の問題へ」と「もう一度」の両方に使います。  
4つの選択肢ボタンは配列にまとめ、`for` 文で順番に処理します。

</Action>

</Section>

---

<Section title="現在の問題を表示する" goal="currentIndexに対応する問題文と選択肢を画面へ表示します。">

`questions[currentIndex]` で、現在の問題を取り出します。

| コード | 取り出す値 |
| --- | --- |
| `questions[0]` | 1問目のオブジェクト |
| `questions[0].text` | 1問目の問題文 |
| `questions[0].choices[2]` | 1問目の3番目の選択肢 |

画面に表示する問題番号は、人が数える番号に合わせる必要があります。  
選択肢は、`for` 文のカウンター変数を使って、ボタンと選択肢を順番に対応させます。

### 演習1

<Exercise>

問題が変わるたびに同じ表示処理を使えるよう、現在の問題を表示する処理を `showQuestion` 関数にまとめます。

この関数では、次の3つを画面へ表示します。

1. 現在の問題番号
2. 現在の問題文
3. 4つの選択肢

次の3か所の `___` を完成させてください。

```js
function showQuestion() {
    let question = questions[currentIndex];

    progressText.textContent = ___ + '問目 / ' + questions.length + '問中';
    questionText.textContent = ___;

    for (let i = 0; i < choiceButtons.length; i++) {
        choiceButtons[i].textContent = ___;
    }
}
```

完成した関数を、HTML要素を取得するコードの下へ追加します。

次に、`script.js` の最後で `showQuestion()` を呼び出します。

```js
showQuestion();
```

ページを再読み込みし、次の3点を確認してください。

- 「1問目 / 3問中」と表示される
- 1問目の問題文が表示される
- 4つの選択肢が順番どおりに表示される

<Hint label="ヒント1">

`question` には1問分のデータが入っています。セクション冒頭の表を見返して、問題文と選択肢がどのプロパティに入っているか確認してください。

</Hint>

<Hint label="ヒント2">

- 問題番号は「人が数える番号」にする必要があります。`currentIndex` が `0` のとき、画面に表示する番号は何でしょうか？
- `for` 文のカウンター変数 `i` は、ボタンの番号と選択肢の番号の両方に使えます。

</Hint>

<Answer>

```js
function showQuestion() {
    let question = questions[currentIndex];

    progressText.textContent = (currentIndex + 1) + '問目 / ' + questions.length + '問中';
    questionText.textContent = question.text;

    for (let i = 0; i < choiceButtons.length; i++) {
        choiceButtons[i].textContent = question.choices[i];
    }
}
```

`currentIndex + 1` は表示用の問題番号、`question.text` は問題文、`question.choices[i]` は `i` 番目の選択肢です。

</Answer>

</Exercise>

### 理解確認2

<QuickCheck>

`showQuestion` の `for` 文で `i` が `2` のとき、どのボタンに何が表示されますか。1問目のデータを使って答えてください。

<Hint>

`choiceButtons[i]` と `question.choices[i]` の `i` には同じ値が入ります。1問目のデータの `choices` 配列を見て、インデックス `2` に何が入っているか確認してください。

</Hint>

<Answer>

3番目のボタンに、1問目の3番目の選択肢である `textContent` が表示されます。

```js
choiceButtons[2].textContent = question.choices[2];
```

</Answer>

</QuickCheck>

<Recovery title="1問目が表示されない場合">

次の箇所を確認してください。

1. `currentIndex` の最初の値が `0` か
2. `showQuestion();` に括弧が付いているか
3. `question.choice` ではなく `question.choices` になっているか
4. `for` 文の条件が `i < choiceButtons.length` になっているか

</Recovery>

</Section>

---

<Section title="選ばれた答えを判定する" goal="選んだインデックスと正解のインデックスを比べ、得点とメッセージを更新します。">

### 演習2

<Exercise>

選ばれた答えを判定する `checkAnswer` 関数を作ります。

この関数には、押された選択肢のインデックスを `selectedIndex` として渡します。  
`selectedIndex` と正解のインデックスを比べて、正解か不正解かを判定します。

次の2か所の `___` を完成させてください。

```js
function checkAnswer(selectedIndex) {
    let question = questions[currentIndex];
    answered = true;

    if (___) {
        score++;
        messageText.textContent = '正解！';
    } else {
        let correctAnswer = ___;
        messageText.textContent = '不正解。正解は「' + correctAnswer + '」です。';
    }
}
```

完成した関数を `showQuestion` 関数の下へ追加します。  
この関数は、次の演習3でボタンのイベント処理を付けると動作確認できます。

<Hint label="ヒント1">

`selectedIndex` は「選んだインデックス」、`question` の中には「正解のインデックス」が入っています。正解かどうかの判定は、この2つのインデックスを比べることです。

</Hint>

<Hint label="ヒント2">

不正解のとき表示する「正解の文章」は、`choices` 配列から取り出します。取り出すときのインデックスは、すでに `question` のプロパティとして持っています。

</Hint>

<Answer>

```js
function checkAnswer(selectedIndex) {
    let question = questions[currentIndex];
    answered = true;

    if (selectedIndex === question.answerIndex) {
        score++;
        messageText.textContent = '正解！';
    } else {
        let correctAnswer = question.choices[question.answerIndex];
        messageText.textContent = '不正解。正解は「' + correctAnswer + '」です。';
    }
}
```

`selectedIndex` は選んだインデックス、`question.answerIndex` は正解のインデックスです。

</Answer>

</Exercise>

### 演習3

<Exercise>

4つのボタンから、押されたインデックスを `checkAnswer` 関数へ渡します。次の4か所の `___` を完成させてください。

```js
choice1.addEventListener('click', function () {
    checkAnswer(___);
});

choice2.addEventListener('click', function () {
    checkAnswer(___);
});

choice3.addEventListener('click', function () {
    checkAnswer(___);
});

choice4.addEventListener('click', function () {
    checkAnswer(___);
});
```

完成したイベント処理を、`script.js` の最後にある `showQuestion();` より上へ追加します。

ページを再読み込みし、次の2点を確認してください。

- 2番目の選択肢を押すと「正解！」と表示される
- それ以外を押すと、不正解と正解の文章が表示される

<Hint>

各ボタンは選択肢の何番目に対応していますか？ `choice1` は1番目の選択肢、`choice2` は2番目の選択肢…と対応しています。配列のインデックスが何番から始まるか思い出してください。

</Hint>

<Answer>

```js
choice1.addEventListener('click', function () {
    checkAnswer(0);
});

choice2.addEventListener('click', function () {
    checkAnswer(1);
});

choice3.addEventListener('click', function () {
    checkAnswer(2);
});

choice4.addEventListener('click', function () {
    checkAnswer(3);
});
```

ボタンの順番と配列のインデックスを同じにすると、選んだインデックスと正解のインデックスを比較できます。

</Answer>

</Exercise>

<Recovery title="選択肢を押しても反応しない場合">

次の箇所を確認してください。

1. `choice1` から `choice4` を正しい `id` で取得しているか
2. 4つの `addEventListener` が `showQuestion();` より上にあるか
3. `checkAnswer(0)` のように括弧を付けているか
4. 比較が `selectedIndex === question.answerIndex` になっているか

</Recovery>

</Section>

---

<Section title="回答後のボタンと色を変更する" goal="回答後は選択肢を押せなくし、正解と不正解を色で示します。">

回答後も選択肢を押せると、同じ問題で `score` が何度も増えてしまいます。  
4つのボタンの `disabled` を `true` にして、押せない状態へ変えます。

### 演習4

<Exercise>

演習2で作った `checkAnswer` 関数へ、次の処理を追加してください。

1. `answered = true;` の下で、4つのボタンを `for` 文で押せない状態にする
2. `if` 文の前で、正解のボタンに `correct` クラスを付ける
3. 不正解の場合は、選んだボタンに `wrong` クラスを付ける

ページを再読み込みし、次の4点を確認してください。

- 正解の選択肢が緑色になる
- 不正解の場合は、選んだ選択肢が赤色になる
- 回答後は4つの選択肢を押せない
- 同じ問題で `score` が2回以上増えない

<Hint label="ヒント1">

- ボタンを押せなくするには、ボタンの `disabled` プロパティを使います。
- CSSの `.correct` や `.wrong` を適用するには、どうやってクラスを付けるか思い出してください。

</Hint>

<Hint label="ヒント2">

- ボタンは4つあり、全部に同じ処理をするときは `for` 文と `choiceButtons` 配列を使います。
- 正解のボタンは `choiceButtons[question.answerIndex]` で特定できます。不正解のボタンは `choiceButtons[selectedIndex]` です。

</Hint>

<Answer>

```js
function checkAnswer(selectedIndex) {
    let question = questions[currentIndex];
    answered = true;

    for (let i = 0; i < choiceButtons.length; i++) {
        choiceButtons[i].disabled = true;
    }

    choiceButtons[question.answerIndex].classList.add('correct');

    if (selectedIndex === question.answerIndex) {
        score++;
        messageText.textContent = '正解！';
    } else {
        choiceButtons[selectedIndex].classList.add('wrong');
        let correctAnswer = question.choices[question.answerIndex];
        messageText.textContent = '不正解。正解は「' + correctAnswer + '」です。';
    }
}
```

回答後はボタンを押せないため、同じ問題で得点がもう一度増えることを防げます。

</Answer>

</Exercise>

</Section>

---

<Section title="次の問題または結果を表示する" goal="回答後に問題番号を進め、次の問題または結果画面を表示します。">

次の問題を表示する前に、選択肢とメッセージを回答前の状態へ戻します。

<Action title="showQuestionに次の問題の準備を追加する">

`showQuestion` の `for` 文へ、次の3行を追加します。

```js
choiceButtons[i].disabled = false;
choiceButtons[i].classList.remove('correct');
choiceButtons[i].classList.remove('wrong');
```

`for` 文の下へ、次の2行を追加します。

```js
messageText.textContent = '選択肢を選んでください。';
answered = false;
```

</Action>

<Action title="結果を表示する関数を書く">

全問終了後の結果画面を表示する関数を作ります。  
`checkAnswer` 関数の下に、次の関数を追加してください。

```js
function showFinalResult() {
    progressText.textContent = '終了';
    questionText.textContent = '結果は ' + questions.length + '問中 ' + score + '問正解です。';
    choices.classList.add('hidden');
    messageText.textContent = 'おつかれさまでした。';
    actionButton.textContent = 'もう一度';
}
```

この関数では、選択肢を `hidden` クラスで非表示にし、画面下の操作ボタンの文字を「もう一度」へ変えています。

</Action>

完成形では、「次の問題へ」を押すと次の問題に切り替わりました。この処理をまず演習5で作ります。

### 演習5

<Exercise>

「次の問題へ」ボタンが押されたときに、次の問題に切り替わる処理を作ります。  
回答前にボタンが押されたときは進めないようにするガード処理も追加します。

次の2か所の `___` を完成させてください。

```js
actionButton.addEventListener('click', function () {
    if (answered === ___) {
        messageText.textContent = '先に選択肢を選んでください。';
        return;
    }

    currentIndex++;
    ___();
});
```

完成した処理を、選択肢ボタンのイベント処理（`choice1`〜`choice4`）より下、`showQuestion();` より上へ追加します。

ページを再読み込みし、次の3点を確認してください。

- 回答前は次の問題へ進めない
- 回答後は2問目、3問目へ進める
- 問題が切り替わるときに、前のアラートや選択肢の色が消え、新しい問題が表示される

<Hint label="ヒント1">

回答前の `answered` は `false` です。

</Hint>

<Hint label="ヒント2">

インデックスを増やしたあとに呼び出すのは、画面に問題や選択肢を表示する関数です。

</Hint>

<Answer>

```js
actionButton.addEventListener('click', function () {
    if (answered === false) {
        messageText.textContent = '先に選択肢を選んでください。';
        return;
    }

    currentIndex++;
    showQuestion();
});
```

回答前は `return` で処理を終了させます。回答済みの場合は `currentIndex` を増やしてから `showQuestion()` を呼び出し、次の問題と選択肢を表示します。

</Answer>

</Exercise>

ひとまず問題が切り替わるようになりましたが、このままでは問題があります。実際に試してみましょう。

<Verify title="3問目のあとに動作させてみる">

実装したクイズを最後まで進めてみてください。

1. 1問目、2問目を回答して「次の問題へ」を押す
2. 3問目に回答する
3. 3問目の回答後に、もう一度「次の問題へ」を押す

最後のボタンを押したときに、画面の表示がおかしくなったり、画面が動かなくなったりする（エラーになる）ことを確認してください。

</Verify>

### なぜ壊れてしまったのか？

このクイズアプリには問題データが3問しかありません。
`questions` 配列で使えるインデックスは `0`（1問目）、`1`（2問目）、`2`（3問目）の3つだけです。

しかし、3問目の後にさらにボタンを押すと、 `currentIndex` が `3` に増えてしまいます。
すると JavaScript は存在しない `questions[3]` を使おうとしてエラーになり、画面が動かなくなってしまいます。

そうならないために、全問終わったときは次の問題に進むのではなく、**代わりに結果画面を出す**ように修正する必要があります。

### 演習6

<Exercise>

演習5で作った `actionButton` のイベント処理を書き換えて、次の問題が残っているかどうかで条件分岐するようにします。
次の3か所の `___` を完成させてください。

```js
actionButton.addEventListener('click', function () {
    if (answered === false) {
        messageText.textContent = '先に選択肢を選んでください。';
        return;
    }

    currentIndex++;

    if (currentIndex ___ questions.length) {
        ___();
    } else {
        ___();
    }
});
```

ページを再読み込みし、次の2点を確認してください。

- 3問目を回答したあとにボタンを押すと、結果画面（「終了」）に変わる
- 結果画面では選択肢が消え、一番下のボタンが「もう一度」に変わる

<Hint label="ヒント1">

3問の配列で使えるインデックスは `0`〜`2` です。`currentIndex` が `3` のとき（`questions.length` と同じ数値のとき）は問題がないため、それより小さい（未満の）場合だけ次の問題を表示します。

</Hint>

<Hint label="ヒント2">

`if` の中と `else` の中で、問題を表示する関数（`showQuestion`）と結果を表示する関数（`showFinalResult`）を使い分けます。

</Hint>

<Answer>

```js
actionButton.addEventListener('click', function () {
    if (answered === false) {
        messageText.textContent = '先に選択肢を選んでください。';
        return;
    }

    currentIndex++;

    if (currentIndex < questions.length) {
        showQuestion();
    } else {
        showFinalResult();
    }
});
```

`currentIndex` が `questions.length` より小さい場合は `showQuestion()` で次の問題を表示し、同じかそれ以上になった場合は `showFinalResult()` で結果画面を表示します。

</Answer>

</Exercise>

<Recovery title="次の問題へ進めない、または結果画面にならない場合">

次の箇所を確認してください。

1. 比較する条件が `currentIndex < questions.length` になっているか
2. `showQuestion();` と `showFinalResult();` に括弧が付いているか
3. `currentIndex++` の位置が条件判定より前になっているか

</Recovery>

</Section>

---

<Section title="同じボタンでもう一度遊ぶ" goal="結果画面のボタンで、問題番号と得点をリセットして1問目へ戻ります。">

再スタートするときは、`showQuestion` でクイズ画面を元に戻します。

<Action title="showQuestionに表示を戻す処理を追加する">

`showQuestion` の `for` 文の下へ、次の2行を追加します。

```js
choices.classList.remove('hidden');
actionButton.textContent = '次の問題へ';
```

これで、再スタート時に選択肢を再表示し、ボタンの文字を元へ戻せます。

</Action>

### 演習7

<Exercise>

結果画面でボタンが押された場合に、最初からやり直す処理を追加します。  
「今が結果画面かどうか」を判定する条件と、リセットする変数を考えてください。

演習6で作った `actionButton` のイベント処理を、次のコードへ置き換えてください。  
追加した3か所の `___` を完成させます。

```js
actionButton.addEventListener('click', function () {
    if (currentIndex === ___) {
        currentIndex = ___;
        score = ___;
        showQuestion();
        return;
    }

    if (answered === false) {
        messageText.textContent = '先に選択肢を選んでください。';
        return;
    }

    currentIndex++;

    if (currentIndex < questions.length) {
        showQuestion();
    } else {
        showFinalResult();
    }
});
```

次の4点を確認してください。

- 結果画面でボタンの文字が「もう一度」になる
- 「もう一度」を押すと1問目へ戻る
- 得点が0点へ戻る
- ボタンの文字が「次の問題へ」に戻る

<Hint label="ヒント1">

「もう一度」ボタンが押されたかどうかを判定するには、全問終了後にしかなりえない `currentIndex` の値を使います。演習6で、最後の問題のあとにこの変数がどうなるか確認してください。

</Hint>

<Hint label="ヒント2">

最初からやり直すため、最初の状態に戻したい変数は2つあります。セクション「クイズの進み具合を変数で覚える」の表で、それぞれの「最初の値」を確認してください。

</Hint>

<Answer>

```js
actionButton.addEventListener('click', function () {
    if (currentIndex === questions.length) {
        currentIndex = 0;
        score = 0;
        showQuestion();
        return;
    }

    if (answered === false) {
        messageText.textContent = '先に選択肢を選んでください。';
        return;
    }

    currentIndex++;

    if (currentIndex < questions.length) {
        showQuestion();
    } else {
        showFinalResult();
    }
});
```

最初の `if` 文は、結果画面でボタンが押された場合だけ実行されます。  
問題番号と得点を戻してから `showQuestion()` を呼び出し、そこで選択肢とボタンの文字も元へ戻します。

</Answer>

</Exercise>

<Checkpoint title="基本機能の完成確認">

次の条件をすべて満たしていれば完成です。

- 回答前は次の問題へ進めない
- 正解した問題だけ得点が増える
- 回答後は選択肢を押せない
- 正解と不正解が色で分かる
- 3問を順番に表示できる
- 最後に正解数が表示される
- 同じボタンで1問目・0点から再開できる

</Checkpoint>

<Recovery title="再スタートできない場合">

次の箇所を確認してください。

1. 全問終了後を `currentIndex === questions.length` で判定しているか
2. `currentIndex` と `score` を両方 `0` へ戻しているか
3. 再スタート時に `showQuestion();` を呼び出しているか
4. `showQuestion` で `hidden` クラスを外し、ボタンの文字を戻しているか

</Recovery>

</Section>

---

<Section title="自分の4択クイズに改造する" goal="questions配列を書き換え、自分で考えた問題を出題します。">

### 演習8

<Exercise>

完成した処理はそのまま使い、`questions` 配列の中身を自分のテーマへ変更します。

ゲーム、食べ物、地理、動物、学校生活、JavaScriptの復習などから、答えが事実として決まるテーマを選んでください。

`questions` 配列を、選んだテーマの4択クイズへ書き換えてください。

次の条件をすべて満たせば成功です。

- 問題を3問以上作る
- 各問題に4つの選択肢がある
- 問題文と正解が事実として正しい
- `answerIndex` が正解のインデックスと一致している
- 正解のインデックスが毎回同じにならない
- 全問を操作し、表示と得点が正しいことを確認する

<Hint>

変更するのは、各問題の `text`、`choices`、`answerIndex` です。  
選択肢を並べ替えたときは、正解のインデックスを `0` から数え直します。

</Hint>

<Answer>

1問分の例は次のとおりです。この形で、同じテーマの問題を3問以上作ります。

```js
{
    text: '日本で一番高い山は？',
    choices: ['高尾山', '富士山', '筑波山', '阿蘇山'],
    answerIndex: 1
}
```

正解の富士山は2番目にあるため、`answerIndex` は `1` です。

</Answer>

</Exercise>

<Checkpoint title="自作クイズの完成確認">

次の4点を確認してください。

- 自分の問題が3問以上ある
- 各問題に選択肢が4つある
- 問題文と正解が事実として正しい
- `answerIndex` と正解のインデックスが一致している

</Checkpoint>

### 演習-発展1

<Exercise>

各問題に、正解を説明する `explanation` プロパティを追加してください。  
正解・不正解のどちらの場合も、回答後に説明を表示します。

次の条件をすべて満たせば成功です。

- すべての問題に `explanation` がある
- 正解・不正解のどちらでも説明が表示される
- 次の問題へ進むと、前の説明が残らない

<Hint>

問題データに新しいプロパティを追加するのは、既存のプロパティ（`text` や `choices`）と同じ書き方です。追加したプロパティを、回答後のメッセージに文字列結合で付け足してみてください。

</Hint>

<Answer>

問題データに `explanation` を追加します。

```js
{
    text: '配列の最初の要素を取り出すインデックスは？',
    choices: ['0', '1', '2', '3'],
    answerIndex: 0,
    explanation: '配列のインデックスは0から始まるため、最初の要素は0です。'
}
```

`checkAnswer` で結果の文字を設定したあとに、次のコードを追加します。

```js
messageText.textContent = messageText.textContent + ' ' + question.explanation;
```

`showQuestion` がメッセージを戻すため、次の問題に前の説明は残りません。

</Answer>

</Exercise>

### 演習-発展2

<Exercise>

結果画面のコメントを、正解数によって次のように変えてください。

- 全問正解なら「完璧！」
- 半分以上正解なら「いい感じ！」
- 半分未満なら「もう一度挑戦しよう！」

3種類すべての結果を試し、コメントが条件どおりに変われば成功です。

<Hint>

条件が重なるため、「全問正解」「半分以上」「半分未満」の順番で判定します。

</Hint>

<Answer>

`showFinalResult` の中で、表示するコメントを決めます。

```js
let comment = '';

if (score === questions.length) {
    comment = '完璧！';
} else if (score >= questions.length / 2) {
    comment = 'いい感じ！';
} else {
    comment = 'もう一度挑戦しよう！';
}

messageText.textContent = comment;
```

現在の `messageText.textContent = 'おつかれさまでした。';` と置き換えます。

</Answer>

</Exercise>

</Section>

