---
title: 4択クイズアプリを作ろう
---

import Exercise, { QuickCheck, Hint, Answer } from '@metyatech/exercise/client';

4つの選択肢から答えを選び、最後に正解数が表示されるクイズアプリを作ります。

このページでは、完成コードを一度に書くのではなく、次の順番でJavaScriptを組み立てます。

1. 完成形の動きを確認する
2. HTMLとCSSを用意する
3. 問題データと、クイズの進み具合を表す変数を用意する
4. 現在の問題を表示する
5. 選ばれた答えを判定する
6. 次の問題、結果画面、再スタートを作る
7. 自分のテーマのクイズに改造する

演習1〜5が基本課題です。`演習-発展` は、基本課題を完成させた人だけ取り組みます。

---

<Section title="今日のミッション" goal="複数の問題を順番に表示し、回答と得点を管理するクイズアプリを完成させます。">

完成すると、4つの選択肢から1つを選び、全問が終わったあとに正解数を確認できます。  
結果画面の「もう一度」ボタンを押すと、最初からやり直せます。

<CodePreview
  sourceId="quiz-app-4choice-final-preview"
  htmlVisible={false}
  cssVisible={false}
  jsVisible={false}
  previewVisible={true}
  share={false}
>

```html
<main class="app">
    <h1>4択クイズアプリ</h1>

    <section class="quiz-box">
        <p id="progressText">1問目</p>
        <h2 id="questionText">問題文が入ります</h2>

        <div id="choices" class="choices">
            <button id="choice1">選択肢1</button>
            <button id="choice2">選択肢2</button>
            <button id="choice3">選択肢3</button>
            <button id="choice4">選択肢4</button>
        </div>

        <p id="messageText">選択肢を選んでください。</p>
    </section>

    <button id="nextButton">次の問題へ</button>
    <button id="restartButton">もう一度</button>
</main>
```

```css
* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: sans-serif;
    background: #f6f7fb;
    color: #222222;
}

.app {
    width: 92%;
    max-width: 760px;
    margin: 32px auto;
    padding: 24px;
    background: white;
    border-radius: 20px;
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.08);
}

.quiz-box {
    padding: 20px;
    border-radius: 16px;
    background: #f0f0f0;
}

#progressText {
    font-weight: bold;
}

#questionText {
    min-height: 64px;
}

.choices button {
    display: block;
    width: 100%;
    margin-top: 12px;
}

button {
    padding: 14px;
    border: 2px solid transparent;
    border-radius: 12px;
    font-size: 16px;
    cursor: pointer;
}

button:hover {
    border-color: #555555;
}

button:disabled {
    cursor: not-allowed;
    opacity: 0.75;
}

#nextButton,
#restartButton {
    display: block;
    width: 100%;
    margin-top: 20px;
    border-radius: 999px;
}

#restartButton {
    display: none;
}

.correct {
    background: #ccebd3;
    color: #123b1d;
}

.wrong {
    background: #f3c8cc;
    color: #50151b;
}
```

```js
let questions = [
    {
        text: '次のうち、HTML要素にイベント処理を登録するメソッドは？',
        choices: ['querySelector', 'addEventListener', 'textContent', 'console.log'],
        answerIndex: 1
    },
    {
        text: '配列の最初の要素を取り出すインデックスは？',
        choices: ['0', '1', '2', '3'],
        answerIndex: 0
    },
    {
        text: '次のうち、HTML要素の文字を取得・設定できるプロパティは？',
        choices: ['value', 'length', 'textContent', 'querySelector'],
        answerIndex: 2
    }
];

let currentIndex = 0;
let score = 0;
let answered = false;

let progressText = document.querySelector('#progressText');
let questionText = document.querySelector('#questionText');
let choices = document.querySelector('#choices');
let messageText = document.querySelector('#messageText');
let nextButton = document.querySelector('#nextButton');
let restartButton = document.querySelector('#restartButton');

let choice1 = document.querySelector('#choice1');
let choice2 = document.querySelector('#choice2');
let choice3 = document.querySelector('#choice3');
let choice4 = document.querySelector('#choice4');

let choiceButtons = [choice1, choice2, choice3, choice4];

function showQuestion() {
    let question = questions[currentIndex];

    progressText.textContent = (currentIndex + 1) + '問目 / ' + questions.length + '問中';
    questionText.textContent = question.text;

    for (let i = 0; i < choiceButtons.length; i++) {
        choiceButtons[i].textContent = question.choices[i];
        choiceButtons[i].disabled = false;
        choiceButtons[i].className = '';
    }

    choices.style.display = 'block';
    messageText.textContent = '選択肢を選んでください。';
    messageText.className = '';
    nextButton.style.display = 'block';
    restartButton.style.display = 'none';
    answered = false;
}

function checkAnswer(selectedIndex) {
    if (answered === true) {
        return;
    }

    let question = questions[currentIndex];
    answered = true;

    for (let i = 0; i < choiceButtons.length; i++) {
        choiceButtons[i].disabled = true;
    }

    choiceButtons[question.answerIndex].className = 'correct';

    if (selectedIndex === question.answerIndex) {
        score++;
        messageText.textContent = '正解！';
        messageText.className = 'correct';
    } else {
        choiceButtons[selectedIndex].className = 'wrong';
        let correctAnswer = question.choices[question.answerIndex];
        messageText.textContent = '不正解。正解は「' + correctAnswer + '」です。';
        messageText.className = 'wrong';
    }
}

function showFinalResult() {
    progressText.textContent = '終了';
    questionText.textContent = '結果は ' + questions.length + '問中 ' + score + '問正解です。';
    choices.style.display = 'none';
    messageText.textContent = 'おつかれさまでした。';
    messageText.className = '';
    nextButton.style.display = 'none';
    restartButton.style.display = 'block';
}

choice1.addEventListener('click', function () {
    checkAnswer(0);
});

choice2.addEventListener('click', function () {
    checkAnswer(1);
});

choice3.addEventListener('click', function () {
    checkAnswer(2);
});

choice4.addEventListener('click', function () {
    checkAnswer(3);
});

nextButton.addEventListener('click', function () {
    if (answered === false) {
        messageText.textContent = '先に選択肢を選んでください。';
        return;
    }

    currentIndex++;

    if (currentIndex < questions.length) {
        showQuestion();
    } else {
        showFinalResult();
    }
});

restartButton.addEventListener('click', function () {
    currentIndex = 0;
    score = 0;
    showQuestion();
});

showQuestion();
```

</CodePreview>

### 演習1

<Exercise>

完成形を動かし、次のすべてを確認してください。

1. 回答前に「次の問題へ」を押すと、先に回答するよう表示される
2. 選択肢を押すと、正解または不正解が表示される
3. 回答後は、正解の選択肢が緑色になる
4. 不正解の場合は、選んだ選択肢が赤色になる
5. 回答後は、ほかの選択肢を押せない
6. 最後まで進むと正解数が表示される
7. 「もう一度」を押すと、1問目・0点から再開する

7つの動きをすべて確認できれば成功です。

<Hint>

1問目では、正解を選ぶ場合と不正解を選ぶ場合を別々に試してください。  
そのあと、回答せずに「次の問題へ」を押す場合、最後まで回答する場合、「もう一度」を押す場合を順番に確認します。

</Hint>

<Answer>

完成形では、次のように動きます。

- 回答前は次の問題へ進めない
- 回答すると正誤と正解の選択肢が分かる
- 回答後は同じ問題にもう一度回答できない
- 全問が終わると正解数が表示される
- 「もう一度」を押すと、問題番号と得点が最初の値へ戻る

これから、この動きを「問題の表示」「回答の判定」「問題を進める処理」に分けて作ります。

</Answer>

</Exercise>

</Section>

---

<Section title="HTMLとCSSを用意する" goal="JavaScriptから書き換える場所と、クイズの見た目を用意します。">

このページではJavaScriptの処理に集中するため、HTMLとCSSはそのままコピーして使ってかまいません。

HTMLでは、JavaScriptから取得する要素に `id` を付けます。

CSSでは、4つの選択肢ボタンを `display: block` で縦に並べます。  
「もう一度」ボタンは、最初は `display: none` で非表示にします。

`button:disabled` は、`disabled = true` で押せない状態になったボタンだけを指定する疑似クラスです。  
このアプリでは、回答後のボタンを少し薄く表示するために使います。

<Action title="HTMLを書く">

`index.html` の `<body>` の中に、次のHTMLを書きます。

```html
<main class="app">
    <h1>4択クイズアプリ</h1>

    <section class="quiz-box">
        <p id="progressText">1問目</p>
        <h2 id="questionText">問題文が入ります</h2>

        <div id="choices" class="choices">
            <button id="choice1">選択肢1</button>
            <button id="choice2">選択肢2</button>
            <button id="choice3">選択肢3</button>
            <button id="choice4">選択肢4</button>
        </div>

        <p id="messageText">選択肢を選んでください。</p>
    </section>

    <button id="nextButton">次の問題へ</button>
    <button id="restartButton">もう一度</button>
</main>
```

</Action>

<Action title="CSSとJavaScriptを読み込む">

`index.html` の `<head>` の中に、次の2行を追加します。

```html
<link rel="stylesheet" href="style.css">
<script src="script.js" defer></script>
```

`defer` を付けると、HTMLの読み込みが終わったあとでJavaScriptが実行されます。

</Action>

<Action title="CSSを書く">

`style.css` に次のCSSを書きます。

```css
* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: sans-serif;
    background: #f6f7fb;
    color: #222222;
}

.app {
    width: 92%;
    max-width: 760px;
    margin: 32px auto;
    padding: 24px;
    background: white;
    border-radius: 20px;
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.08);
}

.quiz-box {
    padding: 20px;
    border-radius: 16px;
    background: #f0f0f0;
}

#progressText {
    font-weight: bold;
}

#questionText {
    min-height: 64px;
}

.choices button {
    display: block;
    width: 100%;
    margin-top: 12px;
}

button {
    padding: 14px;
    border: 2px solid transparent;
    border-radius: 12px;
    font-size: 16px;
    cursor: pointer;
}

button:hover {
    border-color: #555555;
}

button:disabled {
    cursor: not-allowed;
    opacity: 0.75;
}

#nextButton,
#restartButton {
    display: block;
    width: 100%;
    margin-top: 20px;
    border-radius: 999px;
}

#restartButton {
    display: none;
}

.correct {
    background: #ccebd3;
    color: #123b1d;
}

.wrong {
    background: #f3c8cc;
    color: #50151b;
}
```

</Action>

<Verify title="画面を確認する">

問題文、4つの選択肢、「次の問題へ」ボタンが表示されていれば成功です。

「もう一度」ボタンが表示されていないことも確認してください。  
この時点では、ボタンを押してもまだ何も起こりません。

</Verify>

<Recovery title="見た目が完成例と違う場合">

次の順番で確認してください。

1. `index.html` の `<head>` に `style.css` を読み込む行があるか
2. ファイル名が `style.css` と一致しているか
3. HTMLの閉じタグやCSSの波かっこが抜けていないか
4. 開発者ツールのコンソールにエラーが表示されていないか

最初に表示されているエラーから1つずつ直します。

</Recovery>

</Section>

---

<Section title="問題データを用意する" goal="問題文、4つの選択肢、正解の位置を、配列とオブジェクトでまとめます。">

1問分のデータを、次の3つのプロパティを持つオブジェクトにします。

| プロパティ | 入れる値 |
| --- | --- |
| `text` | 問題文 |
| `choices` | 4つの選択肢を入れた配列 |
| `answerIndex` | 正解が入っている位置を表すインデックス |

配列のインデックスは `0` から始まります。たとえば正解が2番目の選択肢なら、`answerIndex` は `1` です。

<Action title="questions配列を書く">

空の `script.js` の先頭に、次のコードを書きます。

```js
let questions = [
    {
        text: '次のうち、HTML要素にイベント処理を登録するメソッドは？',
        choices: ['querySelector', 'addEventListener', 'textContent', 'console.log'],
        answerIndex: 1
    },
    {
        text: '配列の最初の要素を取り出すインデックスは？',
        choices: ['0', '1', '2', '3'],
        answerIndex: 0
    },
    {
        text: '次のうち、HTML要素の文字を取得・設定できるプロパティは？',
        choices: ['value', 'length', 'textContent', 'querySelector'],
        answerIndex: 2
    }
];
```

</Action>

### 理解確認1

<QuickCheck>

次の問題では、正解は4番目の選択肢です。`answerIndex` に入れる数値を答えてください。

```js
{
    text: '配列の要素数を表すプロパティは？',
    choices: ['value', 'textContent', 'querySelector', 'length'],
    answerIndex: ___
}
```

正解が `length` になる数値を答えられれば成功です。

<Hint>

配列の位置は、1番目から順に `0`、`1`、`2`、`3` と数えます。

</Hint>

<Answer>

入れる数値は `3` です。

`length` は4番目にあります。配列のインデックスは `0` から始まるため、4番目のインデックスは `3` です。

```js
{
    text: '配列の要素数を表すプロパティは？',
    choices: ['value', 'textContent', 'querySelector', 'length'],
    answerIndex: 3
}
```

</Answer>

</QuickCheck>

</Section>

---

<Section title="クイズの進み具合を変数で覚える" goal="現在の問題番号、正解数、回答済みかどうかを変数で管理します。">

複数問題のクイズでは、アプリが今どのような状況なのかを覚える必要があります。  
このような、今のアプリの状況を表す値を「状態」と呼びます。

| 変数 | 最初の値 | 役割 |
| --- | ---: | --- |
| `currentIndex` | `0` | 今表示している問題のインデックス |
| `score` | `0` | これまでの正解数 |
| `answered` | `false` | 今の問題に回答済みかどうか |

<Action title="状態を表す変数を書く">

`questions` 配列の下に、次のコードを追加します。

```js
let currentIndex = 0;
let score = 0;
let answered = false;
```

</Action>

<Action title="HTML要素を取得する">

状態を表す変数の下に、次のコードを追加します。

```js
let progressText = document.querySelector('#progressText');
let questionText = document.querySelector('#questionText');
let choices = document.querySelector('#choices');
let messageText = document.querySelector('#messageText');
let nextButton = document.querySelector('#nextButton');
let restartButton = document.querySelector('#restartButton');

let choice1 = document.querySelector('#choice1');
let choice2 = document.querySelector('#choice2');
let choice3 = document.querySelector('#choice3');
let choice4 = document.querySelector('#choice4');

let choiceButtons = [choice1, choice2, choice3, choice4];
```

4つのボタンを `choiceButtons` 配列にまとめると、`for` 文で4つすべてを順番に処理できます。

</Action>

</Section>

---

<Section title="現在の問題を画面に表示する" goal="currentIndexに対応する問題文と4つの選択肢を画面へ表示します。">

`questions[currentIndex]` で、今表示する問題のオブジェクトを取り出します。

たとえば `currentIndex` が `0` のとき、次のように値をたどります。

| コード | 取り出す値 |
| --- | --- |
| `questions[0]` | 1問目のオブジェクト |
| `questions[0].text` | 1問目の問題文 |
| `questions[0].choices[2]` | 1問目の3番目の選択肢 |

問題番号は、人が数える番号に合わせるため、`currentIndex + 1` と表示します。  
括弧を付けた `(currentIndex + 1)` を先に計算してから、「問目」という文字につなげます。

4つのボタンへ選択肢を入れるときは、同じ `i` を2つの配列に使います。

| `i` | 書き換えるボタン | 入れる選択肢 |
| ---: | --- | --- |
| `0` | `choiceButtons[0]` | `question.choices[0]` |
| `1` | `choiceButtons[1]` | `question.choices[1]` |
| `2` | `choiceButtons[2]` | `question.choices[2]` |
| `3` | `choiceButtons[3]` | `question.choices[3]` |

ボタン要素の `disabled` に `false` を入れると、ボタンを押せる状態になります。  
要素の `className` に空文字 `''` を入れると、前の問題で付けた `correct` または `wrong` というクラス名を取り除けます。

### 演習2

<Exercise>

次の `showQuestion` 関数にある3か所の `___` を完成させてください。

```js
function showQuestion() {
    let question = questions[currentIndex];

    progressText.textContent = ___ + '問目 / ' + questions.length + '問中';
    questionText.textContent = ___;

    for (let i = 0; i < choiceButtons.length; i++) {
        choiceButtons[i].textContent = ___;
        choiceButtons[i].disabled = false;
        choiceButtons[i].className = '';
    }

    choices.style.display = 'block';
    messageText.textContent = '選択肢を選んでください。';
    messageText.className = '';
    nextButton.style.display = 'block';
    restartButton.style.display = 'none';
    answered = false;
}
```

完成した関数を要素取得のコードの下に書き、`script.js` の一番下へ次の1行を追加します。

```js
showQuestion();
```

ページを再読み込みし、次のすべてを確認できれば成功です。

- 「1問目 / 3問中」と表示される
- 1問目の問題文が表示される
- 4つの選択肢が順番どおりに表示される

<Hint>

- 表示する問題番号は、`currentIndex` に `1` を足します。
- 問題文は、現在の問題を入れた `question` の `text` です。
- `i` 番目のボタンには、`question.choices` の `i` 番目を入れます。

</Hint>

<Answer>

3か所は、次のようにします。

```js
function showQuestion() {
    let question = questions[currentIndex];

    progressText.textContent = (currentIndex + 1) + '問目 / ' + questions.length + '問中';
    questionText.textContent = question.text;

    for (let i = 0; i < choiceButtons.length; i++) {
        choiceButtons[i].textContent = question.choices[i];
        choiceButtons[i].disabled = false;
        choiceButtons[i].className = '';
    }

    choices.style.display = 'block';
    messageText.textContent = '選択肢を選んでください。';
    messageText.className = '';
    nextButton.style.display = 'block';
    restartButton.style.display = 'none';
    answered = false;
}
```

- `(currentIndex + 1)` は、人が見る問題番号です。
- `question.text` は、現在の問題文です。
- `question.choices[i]` は、現在の問題の `i` 番目の選択肢です。

</Answer>

</Exercise>

### 理解確認2

<QuickCheck>

`showQuestion` の `for` 文で `i` が `2` のとき、どのボタンに、どの選択肢が入りますか。

1問目のデータを使って説明できれば成功です。

<Hint>

配列のインデックス `2` は、3番目の要素です。

</Hint>

<Answer>

次の処理になります。

```js
choiceButtons[2].textContent = question.choices[2];
```

`choiceButtons[2]` は3番目のボタン、`question.choices[2]` は3番目の選択肢です。  
1問目では、3番目のボタンに `textContent` と表示されます。

</Answer>

</QuickCheck>

<Recovery title="1問目が表示されない場合">

開発者ツールのコンソールを開き、最初のエラーを確認します。  
そのうえで、次の箇所を順番に見比べてください。

1. `currentIndex` の最初の値が `0` か
2. `showQuestion();` の括弧が付いているか
3. HTMLの `id` と `querySelector` の文字が一致しているか
4. `question.choice` ではなく `question.choices` になっているか
5. `i <= choiceButtons.length` ではなく `i < choiceButtons.length` になっているか

直したあと、ページを再読み込みして確認します。

</Recovery>

</Section>

---

<Section title="選ばれた答えを判定する" goal="選ばれた位置と正解の位置を比べ、得点と画面表示を更新します。">

`checkAnswer` 関数には、押された選択肢の位置を `selectedIndex` として渡します。

- 1番目のボタンなら `0`
- 2番目のボタンなら `1`
- 3番目のボタンなら `2`
- 4番目のボタンなら `3`

現在の問題が持っている `answerIndex` と `selectedIndex` が同じなら正解です。

回答後は、次の順番で処理します。

1. `answered` を `true` にする
2. 4つのボタンを押せない状態にする
3. 正解の選択肢へ `correct` クラスを付ける
4. 選ばれた位置と正解の位置を比べる
5. 正解なら得点を増やし、不正解なら選んだ選択肢へ `wrong` クラスを付ける

関数の途中で `return` を実行すると、その時点で関数の処理が終了します。  
すでに回答済みなら、得点や表示を変更せずに終了させます。

### 演習3

<Exercise>

次の `checkAnswer` 関数にある3か所の `___` を完成させてください。

```js
function checkAnswer(selectedIndex) {
    if (answered === true) {
        return;
    }

    let question = questions[currentIndex];
    answered = true;

    for (let i = 0; i < choiceButtons.length; i++) {
        choiceButtons[i].disabled = true;
    }

    choiceButtons[question.answerIndex].className = 'correct';

    if (___) {
        score++;
        messageText.textContent = '正解！';
        messageText.className = 'correct';
    } else {
        choiceButtons[___].className = 'wrong';
        let correctAnswer = ___;
        messageText.textContent = '不正解。正解は「' + correctAnswer + '」です。';
        messageText.className = 'wrong';
    }
}
```

完成した関数を `showQuestion` 関数の下へ追加します。

次の条件を満たすコードになれば成功です。

- 選ばれた位置と正解の位置が同じときだけ `score` が増える
- 不正解の場合は、選んだボタンが赤色になる
- 不正解の場合は、正解の文字を表示できる

<Hint>

- 正解かどうかは、`selectedIndex` と `question.answerIndex` を比較します。
- 赤色にするのは、学習者が選んだ位置のボタンです。
- 正解の文字は、`question.choices` の正解位置にあります。

</Hint>

<Answer>

3か所は、次のようにします。

```js
function checkAnswer(selectedIndex) {
    if (answered === true) {
        return;
    }

    let question = questions[currentIndex];
    answered = true;

    for (let i = 0; i < choiceButtons.length; i++) {
        choiceButtons[i].disabled = true;
    }

    choiceButtons[question.answerIndex].className = 'correct';

    if (selectedIndex === question.answerIndex) {
        score++;
        messageText.textContent = '正解！';
        messageText.className = 'correct';
    } else {
        choiceButtons[selectedIndex].className = 'wrong';
        let correctAnswer = question.choices[question.answerIndex];
        messageText.textContent = '不正解。正解は「' + correctAnswer + '」です。';
        messageText.className = 'wrong';
    }
}
```

`selectedIndex` は選んだ位置、`question.answerIndex` は正解の位置です。  
不正解の場合でも、正解の位置は `question.answerIndex` から取り出せます。

</Answer>

</Exercise>

<Action title="4つのボタンと判定関数をつなぐ">

`script.js` の末尾にある `showQuestion();` より上へ、次のイベント処理を追加します。

```js
choice1.addEventListener('click', function () {
    checkAnswer(0);
});

choice2.addEventListener('click', function () {
    checkAnswer(1);
});

choice3.addEventListener('click', function () {
    checkAnswer(2);
});

choice4.addEventListener('click', function () {
    checkAnswer(3);
});
```

それぞれのボタンから、ボタンの位置を表す `0`、`1`、`2`、`3` を `checkAnswer` に渡しています。

</Action>

<Verify title="正解判定を確認する">

ページを再読み込みし、次のすべてを確認してください。

- 正解なら「正解！」と表示される
- 不正解なら、正解の文字も表示される
- 正解の選択肢が緑色になる
- 不正解の場合は、選んだ選択肢が赤色になる
- 回答後は4つの選択肢を押せない

</Verify>

<Recovery title="選択肢を押しても反応しない場合">

次の順番で確認してください。

1. `choice1` から `choice4` を正しい `id` で取得しているか
2. 4つの `addEventListener` が `showQuestion();` より上にあるか
3. `checkAnswer(0)` のように括弧を付けて関数を呼び出しているか
4. `if (answered === true)` が、代入の `=` ではなく比較の `===` になっているか
5. コンソールの最初のエラーが示す行を確認したか

</Recovery>

</Section>

---

<Section title="次の問題と結果画面を作る" goal="回答後に次の問題へ進み、最後は結果と再スタートボタンを表示します。">

最後の問題を回答したあとには、次の問題がありません。  
その場合は選択肢と「次の問題へ」ボタンを非表示にし、正解数と「もう一度」ボタンを表示します。

<Action title="結果を表示する関数を書く">

`checkAnswer` 関数の下に、次の関数を追加します。

```js
function showFinalResult() {
    progressText.textContent = '終了';
    questionText.textContent = '結果は ' + questions.length + '問中 ' + score + '問正解です。';
    choices.style.display = 'none';
    messageText.textContent = 'おつかれさまでした。';
    messageText.className = '';
    nextButton.style.display = 'none';
    restartButton.style.display = 'block';
}
```

</Action>

次の問題へ進むときは、`currentIndex` を1増やします。

- 増やした値が `questions.length` より小さい場合は、まだ表示する問題がある
- 増やした値が `questions.length` と同じになった場合は、配列の外まで進んでいる

### 理解確認3

<QuickCheck>

3問あるクイズで、最後の問題を回答したあとに `currentIndex` を1増やすと、値は `3` になります。

このとき、次のどちらの条件を使えば「まだ次の問題がある」と正しく判定できますか。

```js
currentIndex < questions.length
```

```js
currentIndex <= questions.length
```

正しい条件と、その理由を説明できれば成功です。

<Hint>

3問の配列で使えるインデックスは `0`、`1`、`2` です。  
`3` は配列の中にあるかを考えてください。

</Hint>

<Answer>

正しい条件は、次のコードです。

```js
currentIndex < questions.length
```

3問の配列で使える最後のインデックスは `2` です。  
`currentIndex` が `3` になった時点で次の問題はないため、`<=` ではなく `<` を使います。

</Answer>

</QuickCheck>

### 演習4

<Exercise>

次のイベント処理にある4か所の `___` を完成させてください。

```js
nextButton.addEventListener('click', function () {
    if (answered === ___) {
        messageText.textContent = '先に選択肢を選んでください。';
        return;
    }

    currentIndex++;

    if (currentIndex ___ questions.length) {
        ___();
    } else {
        ___();
    }
});
```

完成した処理を、4つの選択肢のイベント処理より下、`showQuestion();` より上へ追加します。

次のすべてを確認できれば成功です。

- 回答前は次の問題へ進めない
- 回答後は2問目、3問目へ進める
- 3問目の次は結果画面になる

<Hint>

- 回答前を表す `answered` の値は `false` です。
- 次の問題がある間は、`currentIndex` が `questions.length` より小さい状態です。
- 次の問題があれば `showQuestion`、なければ `showFinalResult` を呼び出します。

</Hint>

<Answer>

4か所は、次のようにします。

```js
nextButton.addEventListener('click', function () {
    if (answered === false) {
        messageText.textContent = '先に選択肢を選んでください。';
        return;
    }

    currentIndex++;

    if (currentIndex < questions.length) {
        showQuestion();
    } else {
        showFinalResult();
    }
});
```

回答前は `return` で処理を終えます。  
回答後は問題番号を進め、配列の中なら次の問題、配列の外なら結果を表示します。

</Answer>

</Exercise>

<Action title="もう一度遊ぶ処理を書く">

`nextButton` のイベント処理の下、`showQuestion();` より上へ、次の処理を追加します。

```js
restartButton.addEventListener('click', function () {
    currentIndex = 0;
    score = 0;
    showQuestion();
});
```

問題番号と得点を `0` に戻してから `showQuestion()` を呼ぶと、1問目から再開できます。  
`showQuestion()` の中で、回答済み状態、選択肢、ボタンの表示も最初の状態へ戻ります。

</Action>

<Checkpoint title="基本機能の完成確認">

次の条件をすべて満たしていれば、基本機能は完成です。

- 回答前は次の問題へ進めない
- 正解した問題だけ `score` が1増える
- 3問を順番に表示できる
- 最後に「3問中○問正解」と表示される
- 結果画面では選択肢と「次の問題へ」が非表示になる
- 「もう一度」を押すと1問目・0点から再開する

</Checkpoint>

<Recovery title="次の問題や結果画面へ進めない場合">

次の箇所を確認してください。

1. `currentIndex++;` が、次の問題の有無を判定する前にあるか
2. 条件が `currentIndex < questions.length` になっているか
3. `showQuestion();` と `showFinalResult();` に括弧が付いているか
4. 回答時に `answered = true`、次の問題表示時に `answered = false` へ変わっているか
5. `showFinalResult` の中で、2つのボタンの表示を正しく切り替えているか

</Recovery>

</Section>

---

<Section title="自分の4択クイズに改造する" goal="questions配列を書き換え、自分で考えた問題を出題します。">

基本の処理はそのまま使い、`questions` 配列の中身だけを自分のテーマへ変更します。

テーマには、ゲーム、食べ物、地理、動物、学校生活、JavaScriptの復習などを選べます。  
答えが事実として決まる問題を作り、意見によって正解が変わる問題は避けてください。

### 演習5

<Exercise>

`questions` 配列を、自分のテーマの4択クイズへ書き換えてください。

次の条件をすべて満たせば成功です。

- 問題を3問以上作る
- 各問題の `choices` に4つの選択肢を入れる
- `answerIndex` が正解の選択肢の位置と一致している
- 正解の位置が毎回同じにならない
- 実際に全問を操作し、表示と得点が正しいことを確認する

<Hint>

変更するのは、各問題の `text`、`choices`、`answerIndex` です。  
選択肢を並べ替えた場合は、正解の位置を `0` から数え直してください。

</Hint>

<Answer>

例として、JavaScriptの復習クイズを作る場合は次のようにできます。

```js
let questions = [
    {
        text: 'CSSセレクタを指定して、最初に一致するHTML要素を取得するメソッドは？',
        choices: ['addEventListener', 'querySelector', 'textContent', 'Number'],
        answerIndex: 1
    },
    {
        text: '配列の要素数を表すプロパティは？',
        choices: ['length', 'value', 'textContent', 'querySelector'],
        answerIndex: 0
    },
    {
        text: 'クリックされたときの処理を登録する場合に指定するイベント名は？',
        choices: ['input', 'keydown', 'click', 'keyup'],
        answerIndex: 2
    }
];
```

1問目は2番目、2問目は1番目、3問目は3番目が正解なので、`answerIndex` は順に `1`、`0`、`2` です。

</Answer>

</Exercise>

<Checkpoint title="完成確認">

次の条件をすべて満たしていれば完成です。

- 自分で決めたテーマの問題が3問以上ある
- 各問題に4つの選択肢がある
- `answerIndex` と正解の位置が一致している
- 回答前は次の問題へ進めない
- 正解と不正解が正しく判定される
- 最後に正解数が表示される
- 「もう一度」で1問目・0点から再開できる

</Checkpoint>

### 演習-発展1

<Exercise>

各問題に、正解を説明する `explanation` プロパティを追加してください。  
正解・不正解のどちらの場合も、回答後に説明を表示します。

次の条件をすべて満たせば成功です。

- すべての問題に `explanation` がある
- 正解・不正解のどちらでも説明が表示される
- 次の問題へ進むと、前の説明が残らない

<Hint>

1問分のオブジェクトへ `explanation` を追加します。  
回答結果の文字を設定したあと、`question.explanation` を `messageText.textContent` へ付け加えます。

</Hint>

<Answer>

問題データに `explanation` を追加します。

```js
{
    text: '配列の最初の要素を取り出すインデックスは？',
    choices: ['0', '1', '2', '3'],
    answerIndex: 0,
    explanation: '配列のインデックスは0から始まるため、最初の要素は0です。'
}
```

`checkAnswer` の正解・不正解を設定したあとに、説明を付け加えます。

```js
messageText.textContent = messageText.textContent + ' ' + question.explanation;
```

`showQuestion` では、`messageText.textContent` を「選択肢を選んでください。」へ戻しているため、次の問題へ進むと前の説明は残りません。

</Answer>

</Exercise>

### 演習-発展2

<Exercise>

結果画面のコメントを、正解数によって次のように変えてください。

- 全問正解なら「完璧！」
- 半分以上正解なら「いい感じ！」
- 半分未満なら「もう一度挑戦しよう！」

3種類すべての結果になる回答を試し、コメントが条件どおりに変われば成功です。

<Hint>

条件が重なる場合は、範囲が最も狭い「全問正解」を最初に判定します。  
そのあとに「半分以上」、最後に「半分未満」を判定します。

</Hint>

<Answer>

`showFinalResult` の中で、表示するコメントを決めます。

```js
let comment = '';

if (score === questions.length) {
    comment = '完璧！';
} else if (score >= questions.length / 2) {
    comment = 'いい感じ！';
} else {
    comment = 'もう一度挑戦しよう！';
}

messageText.textContent = comment;
```

このコードを、現在の `messageText.textContent = 'おつかれさまでした。';` と置き換えます。

</Answer>

</Exercise>

</Section>

---

<Section title="バグ修正チャレンジ" goal="配列、比較、関数呼び出しで起きやすい6個のミスを見つけます。">

ここからは、基本機能を完成させた人向けの発展演習です。

この演習では、コードを実行せずに読み、6個のミスと直し方を書き出します。  
`questions`、`choiceButtons`、`nextButton`、`showFinalResult` は正しく用意され、次のコードより後に初回表示用の `showQuestion();` が正しく書かれているものとします。

### 演習-発展3

<Exercise>

次のコードには、意図的なミスが6個あります。6個すべてを見つけ、正しい書き方を答えてください。

次の観点を1つずつ確認します。

- 1問目から始まるか
- HTMLにある正しい要素を取得しているか
- 4つの選択肢だけを順番に処理しているか
- 問題データの正しいプロパティを使っているか
- 回答済みかどうかを比較しているか
- 次の問題を表示する関数を呼び出しているか

```js
let currentIndex = 1;
let score = 0;
let answered = false;

let progressText = document.querySelector('#progress');
let questionText = document.querySelector('#questionText');

function showQuestion() {
    let question = questions[currentIndex];

    progressText.textContent = (currentIndex + 1) + '問目';
    questionText.textContent = question.text;

    for (let i = 0; i <= choiceButtons.length; i++) {
        choiceButtons[i].textContent = question.choice[i];
    }

    answered = false;
}

function checkAnswer(selectedIndex) {
    if (answered = true) {
        return;
    }

    let question = questions[currentIndex];

    if (selectedIndex === question.answerIndex) {
        score++;
    }

    answered = true;
}

nextButton.addEventListener('click', function () {
    if (answered === false) {
        return;
    }

    currentIndex++;

    if (currentIndex < questions.length) {
        showQuestion;
    } else {
        showFinalResult();
    }
});
```

<Hint>

上から順番に、次の6種類を1つずつ探してください。

1. 最初の問題を表す数値
2. HTMLの `id` とセレクタの文字
3. `for` 文を続ける条件
4. `questions` の各オブジェクトが持つプロパティ名
5. 比較演算子と代入演算子
6. 関数を呼び出すための括弧

</Hint>

<Answer>

6個のミスは次のとおりです。

1. `currentIndex = 1` では2問目から始まるため、`0` にする
2. HTMLの `id="progressText"` と一致するよう、`'#progress'` を `'#progressText'` にする
3. `i <= choiceButtons.length` では存在しない次の要素まで進むため、`i < choiceButtons.length` にする
4. プロパティ名は `choice` ではなく `choices` なので、`question.choices[i]` にする
5. `if (answered = true)` は代入なので、`if (answered === true)` にする
6. `showQuestion;` は関数を呼び出していないため、`showQuestion();` にする

</Answer>

</Exercise>

</Section>
