name: Temporary quiz docs fix
on:
    push:
        paths: ['.github/workflows/fix-quiz-docs-temp.yml']
permissions:
    contents: write
jobs:
    fix:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6
              with:
                  fetch-depth: 0
            - name: Patch and commit
              shell: bash
              run: |
                  python - <<'PY'
                  from pathlib import Path
                  p = Path